# mini-a-tk
mini-a toolkit (agents, slash commands, hooks, skills, etc...)

The toolkit examples track current mini-a behavior, including self-contained `SKILL.yaml` skills and agent files that use `mini-a:` frontmatter overrides for default behavior.
